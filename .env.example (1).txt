# ============================================
# INTELECTUAL IA — variáveis de ambiente
# Copie para .env (local) ou configure no painel do Vercel.
# NUNCA comite o .env real com a chave preenchida.
# ============================================

# Chave do OpenRouter — provedor de texto usado pelo backend (api/chat.js).
# Crie em: https://openrouter.ai/keys
OPENROUTER_API_KEY=
